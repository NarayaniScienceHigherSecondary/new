services:
  - type: web
    name: cms-college-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: GMAIL_USER
        value: highersecondaryschoolnarayanis@gmail.com
      - key: GMAIL_APP_PASSWORD
        value: fciwtkntkzaaeini
